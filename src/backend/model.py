# import sys
# import numpy as np
# # import pandas as pd
# import matplotlib.pyplot as plt
# from fbprophet import Prophet
print("Predicted Value of Energy Consumption is 120 KW")
# date = sys.argv[1]
# time = sys.argv[2]
# filename = "./data.csv"
# df = pd.read_csv(filename)
# df = pd.read_csv(filename, index_col='datetime', parse_dates=['datetime'])
# df['consumption'] = df['consumption'] + 1

# df = df.reset_index()

# df = df.rename(columns={'datetime': 'ds', 'consumption': 'y'})

# model = Prophet()

# model.fit(df)


# datetime = date + " " + time + ":00:00"
# data = ['2018-12-19 12:00:00']
# test = pd.DataFrame(data, columns=['ds'])

# val = model.predict(test)
# print(val['yhat'])
# print("sddf")